# 14. PHP - proměnné \$GLOBALS, \$_SESSION, \$_COOKIE

---

Tyto proměnné označujeme za superglobální proměnné, což znamená že mohou být použity "kdekoliv" v kódu. 

---

## \$GLOBALS

#### Syntax
```php
// Deklarace
$GLOBALS['grandmaCPS'] = 1;
foreach ($GLOBALS["active upgrades"] as $upgrade) {
    if($upgrade->target == "grandma") {
    // Úprava
        $GLOBALS['grandmaCPS'] = $upgrade -> trigger();
    }
} 
```
*...wow co to jen jsou ty šipky :confused: hmmmmmm (bude to v php basics protože mi to přijde důležitý ... jeslti to stihnu)*

---

## \$_SESSION

#### Syntax

```php

```


---

## \$_COOKIE

#### Syntax
```php
// Deklarace proměných pro cookie
$name = "cookies clicked";
$value = 1000;

// ověření existence
if (!isset($_COOKIE[$name])) {
    // tvorba cookie
    setcookie($name, $value, time() + (3600 * 24), "/")
}

// Získání hodnoty cookie
echo $name . ": " . $_COOKIE[$name] . "<br>";

// Změna hodnoty cookie
setcookie($name, $value++, time() + (3600 * 24), "/");

// Smazání cookie
setcookie($name, "", time() -  3600);  

```

---

## Přehled v tabuce

| Vlastnost     | **\$GLOBALS** | **\$_COOKIE**        | **\$_SESSION** |
|---------------|---------------|----------------------|----------------|
| Místo uložení | RAM serveru | prohlížeč uživatele  | Server (identifikována tokenem) |
| Životnost     | Jeden požadavek HTTP | Dlouhodobá (dle nastavení) | Dočasná |
| Bezpečnost    | Nízká (jen pro kód) | Velmi nízká (uživatel je může měnit) | Vysoká |
| Kapacita dat  |Omezena  RAM serveru | Maz 4 KB na jednu cookie | Omezena pouze pamětí serveru |
| Ideální pro   |Konfigurace, Dependency injection | Nastavení vzhedu (theme) | Přihlášení uživatele, nákupní košík |

---

## Ukázka

*budu hostovat na školním hostingu ale momentáně na to nemám kapacitu*